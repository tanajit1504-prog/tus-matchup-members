name: Import website source

on:
  push:
    branches: [main]
    paths: ["tus-matchup-source.zip"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import-source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract website source
        run: |
          cp .github/workflows/deploy-pages.yml /tmp/deploy-pages.yml
          unzip -o tus-matchup-source.zip
          cp /tmp/deploy-pages.yml .github/workflows/deploy-pages.yml
          rm tus-matchup-source.zip
      - name: Commit website files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Import TUS Matchup website source"
          git push
